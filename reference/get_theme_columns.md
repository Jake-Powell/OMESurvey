# Retrieve theme columns

Retrieve theme columns

## Usage

``` r
get_theme_columns(data, theme)
```

## Arguments

- data:

  survey data

- theme:

  A string containing the theme name (with or without "\_" as spacing).

## Value

the column indices of the questions relating to the theme
