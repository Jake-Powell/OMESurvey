% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/survey_summary_functions.R
\name{rename_NA_first_col}
\alias{rename_NA_first_col}
\title{Relabel NA values in the first column of a data frame}
\usage{
rename_NA_first_col(df, missing_label = "Missing")
}
\arguments{
\item{df}{A data frame whose first column may contain \code{NA} values.}

\item{missing_label}{A character string used to replace \code{NA} values
in the first column. Defaults to \code{"Missing"}.}
}
\value{
A data frame with the first column converted to character and
any \code{NA} values replaced by \code{missing_label}.
}
\description{
Converts the first column of a data frame to character and replaces any
\code{NA} values in that column with a specified label (default: "Missing").
Intended for use after \code{tabyl()} and related janitor formatting steps,
once the object has been converted to a plain data frame.
}
\examples{
c(rep("A", 3), rep("B", 2), rep(NA, 2)) |>
  janitor::tabyl() |>
  janitor::adorn_totals() |>
  janitor::adorn_pct_formatting() |>
  as.data.frame() |>
  rename_NA_first_col()

}
