% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{sp_path}
\alias{sp_path}
\title{Build a path within the OME SharePoint root}
\usage{
sp_path(..., root = NULL, must_exist = FALSE)
}
\arguments{
\item{...}{Path components to append to the SharePoint root, passed to
\code{\link[base:file.path]{base::file.path()}}.}

\item{root}{Optional character string giving the SharePoint root directly.
If \code{NULL}, the value is taken from the \code{OME_SHAREPOINT_ROOT} environment
variable.}

\item{must_exist}{Logical. If \code{TRUE}, an error is thrown if the resulting
path does not exist. Default is \code{FALSE}.}
}
\value{
A character string giving the full file path.
}
\description{
Constructs a file path relative to the user's synced SharePoint / OneDrive
root folder stored in the \code{OME_SHAREPOINT_ROOT} environment variable.
}
\details{
This allows scripts to use shared relative paths rather than hard-coded
user-specific absolute paths.
}
\examples{
\dontrun{
sp_path("Folder", "file.csv")
sp_path("ProjectA", "Data", "raw_data.csv", must_exist = TRUE)
}

}
