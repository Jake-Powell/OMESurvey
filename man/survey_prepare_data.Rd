% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/survey_summary_functions.R
\name{survey_prepare_data}
\alias{survey_prepare_data}
\title{Prepare survey data (read, merge, validate) from raw data and dictionary}
\usage{
survey_prepare_data(
  data_path,
  dict_path,
  dict_sheet,
  est_chars_path = NULL,
  est_chars_sheet = NULL,
  est_char_vars = NULL,
  est_char_types = NULL,
  est_char_values = NULL,
  est_char_statements = NULL,
  extra_vars = c("suffix_asis", "keep", "drop")
)
}
\arguments{
\item{data_path}{Character string. Path to the survey data file
(csv, xls, or xlsx).}

\item{dict_path}{Character string. Path to the data dictionary file
(xls or xlsx).}

\item{dict_sheet}{Character string. Name of the sheet within the
data dictionary file to use.}

\item{est_chars_path}{Optional character string. Path to the
establishment characteristics file (xls or xlsx). If \code{NULL}
(default) no additional data are merged.}

\item{est_chars_sheet}{Optional character string. Name of the sheet
within the establishment characteristics file to use. Required if
\code{est_chars_path} is supplied.}

\item{est_char_vars}{Optional character vector. Names of establishment
characteristics variables to include.}

\item{est_char_types}{Optional character vector. Data types for
establishment characteristics variables (same format as dictionary).}

\item{est_char_values}{Optional character vector. Allowed values for
establishment characteristics variables (as in the dictionary).}

\item{est_char_statements}{Optional character vector. Descriptive
statements/labels for establishment characteristics variables.}

\item{extra_vars}{Character string controlling how extra variables present in
the source data but absent from the dictionary are handled in the returned
data. One of \code{"suffix_asis"} (default), \code{"keep"} or \code{"drop"}.
\itemize{
\item \code{"suffix_asis"} appends a suffix _asis to all such variable names
\item \code{"keep"} keeps such variable names as they are, unchanged
\item \code{"drop"} drops all such variables from the returned data
}}
}
\value{
A list with components:
\describe{
\item{data}{A tibble containing the fully prepared survey data,
including merged establishment characteristics (if supplied) and
variables coerced to their specified types. Variable naming conventions
follow \code{\link[=survey_data_prepare]{survey_data_prepare()}}, including the use of \verb{*_raw}, \verb{*_numeric},
and optionally \verb{*_asis} suffixes.}
\item{validation_log}{A list summarising validation checks applied
to each variable.}
\item{validation_df}{A tibble made as \code{dplyr::bind_rows(validation_log)}
(easier to access for some purposes).}
\item{read_messages}{A list of message objects generated during
input reading and merging.}
\item{prep_messages}{A list of message objects generated during
validation and coercion.}
\item{extra_vars}{Character vector of source-data variables not present in
the dictionary.}
\item{extra_vars_mode}{The value of \code{extra_vars} used when preparing the
returned data.}
\item{extra_var_map}{Named character vector mapping original extra-variable
names to their returned names; dropped variables have \code{NA} values.}
}
}
\description{
Convenience wrapper that performs the full survey data preparation
pipeline by combining \code{\link{survey_read_inputs}} and
\code{\link{survey_data_prepare}}. This includes reading input files,
merging establishment characteristics (if supplied), validating and
coercing variables according to the data dictionary, and returning
both processed data and diagnostic outputs.
}
\seealso{
\code{\link{survey_read_inputs}}, \code{\link{survey_data_prepare}}
}
\author{
Dave Sirl
}
