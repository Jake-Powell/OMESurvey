% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/survey_summary_functions.R
\name{OME_stacked_bar_}
\alias{OME_stacked_bar_}
\alias{OME_stacked_bar}
\title{Stacked proportional bar chart in OME style (Dave's version.)}
\usage{
OME_stacked_bar_(
  dat,
  response_var,
  group_var = NULL,
  facet_var = NULL,
  percCut = NULL,
  colo = NULL,
  na.rm = FALSE,
  horiz = FALSE,
  text_scale = 1,
  fillLabText = NULL,
  xLabText = NULL,
  yLabText = "Proportion of responses",
  titleText = NULL,
  facet_labels = NULL,
  facet_layout = NULL,
  fill_label_width = 20
)

OME_stacked_bar(dat, response_var, group_var = NULL, facet_var = NULL, ...)
}
\arguments{
\item{dat}{A data frame.}

\item{response_var}{Bare column name giving the response categories (used for fill).}

\item{group_var}{Optional bare column name defining separate bars.}

\item{facet_var}{Optional bare column name used for faceting.
All variables should be factors (characters may work but are not guaranteed).}

\item{percCut}{Numeric scalar (0–100). Cutoff below which percentages are not
shown in bar segments. Default \code{5}; use a number >100 to suppress all percentages.}

\item{colo}{Optional character vector of fill colours (one per response
level). If \code{NULL} (default), colours are taken from
\code{OMESurvey::get_OME_colours(type = "distinct")}.
When \code{na.rm = FALSE}, a grey colour is prepended for the \code{"Missing"} level.}

\item{na.rm}{Logical. If \code{TRUE}, remove \code{NA} responses; if \code{FALSE} (default)
convert them to \code{"Missing"} and treat as an additional response level.}

\item{horiz}{Logical (default \code{FALSE}). If \code{TRUE}, flip coordinates so bars
are horizontal and place the legend below the plot.}

\item{text_scale}{Positive number (default \code{1}) scaling the size of
percentage and \code{(n)} labels.}

\item{fillLabText}{Optional legend title for the fill variable. If \code{NULL}
(default) the title is removed; if \code{""} the name of \code{response_var} is used.}

\item{xLabText}{Optional label for the bar‑group axis. If \code{NULL} the label is
removed; if \code{""} the name of \code{group_var} is used.}

\item{yLabText}{Label for the proportion axis. Default \code{"Proportion of responses"}.}

\item{titleText}{Optional plot title. Default \code{NULL} removes the title.}

\item{facet_labels}{Optional named character vector used as a labeller for
facets (names are original facet values, values are labels to display).
Default \code{NULL} uses the raw facet values.}

\item{facet_layout}{Optional character. If \code{"1row"} the facets are arranged
in a single row; otherwise the default facet layout is used.}

\item{fill_label_width}{Optional integer. Width (in characters) used when
wrapping fill labels with \code{stringr::str_wrap()}. Default is 20.}

\item{...}{Additional arguments passed to the underlying engine.}
}
\value{
A \code{ggplot} object.
}
\description{
\code{OME_stacked_bar()} builds a stacked (filled) proportional bar chart from
survey-style data using \strong{bare column names}.
\code{OME_stacked_bar_()} is the programmatic interface and underlying engine,
designed for use when column names are supplied as \strong{strings} or \strong{symbols}.
}
\details{
The plot displays:
\itemize{
\item relative frequencies of the response variable,
\item optional subdivision into separate bars,
\item optional faceting,
\item percentage labels inside bar segments (above a cutoff),
\item \code{(n)} labels showing the number of responses per bar.
}

\code{OME_stacked_bar()} uses tidy‑evaluation;
\code{OME_stacked_bar_()} uses standard evaluation and is safe for loops and
programmatic workflows.
}
\note{
Future/possible extensions include adding an optional dashed line
(like \code{OME_boxplot()}), making the \code{(n)} labels optional, using a
secondary axis for \code{(n)} labels, improving percentage‑label contrast for
light fill colours, and a 1‑column option for facet layout.
}
\examples{
dat <- tibble::tibble(
  Response = factor(c("Yes", "No", "No", NA, "Yes", "Maybe")),
  Group    = factor(c("A", "A", "A", "A", "B", "B"))
)

# Basic example (NA is made explicit)
OME_stacked_bar(dat, Response, Group)

# NA's ignored
OME_stacked_bar(dat, Response, Group, na.rm=TRUE)

# Horizontal version with a title
OME_stacked_bar(
   dat,
   response_var = Response,
   group_var = Group,
   horiz = TRUE,
   titleText = "Example bar chart"
)

# Example programmatic use
\dontrun{
  vars <- c("Gender", "Ethnicity", "FSM")
  for (v in vars) {
    p <- OME_stacked_bar_(dat = survey_df, response_var = v)
    print(p)
  }
}

}
\author{
Dave Sirl
}
