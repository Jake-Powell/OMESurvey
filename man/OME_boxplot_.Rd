% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/survey_summary_functions.R
\name{OME_boxplot_}
\alias{OME_boxplot_}
\alias{OME_boxplot}
\title{Boxplot (horizontal), in an OME style.}
\usage{
OME_boxplot_(
  data,
  value_var,
  group_var = NULL,
  show_counts = TRUE,
  na.rm = FALSE,
  count_style = if (na.rm) "non-missing" else "both",
  valueLabText = NULL,
  value_percent_labels = FALSE,
  value_percent_scale = c("percent", "proportion"),
  groupLabText = NULL,
  omitGroupLabels = FALSE,
  titleText = NULL,
  base_size = 14,
  colour = OMESurvey::get_OME_colours(1, type = "sequential"),
  separate_at = NULL,
  group_label_width = NULL,
  group_labels = NULL
)

OME_boxplot(data, value_var, group_var = NULL, ...)
}
\arguments{
\item{data}{A data frame.}

\item{value_var}{Numeric variable to plot.}

\item{group_var}{Optional grouping variable (factor).}

\item{show_counts}{Logical; whether to display (valid/total) counts on the
right-hand axis. Defaults to TRUE.}

\item{na.rm}{Logical. Simpler determination of the value of \code{count_style}.
When \code{TRUE}, \code{count_style} defaults to\code{"non-missing"}; when \code{FALSE} it
defaults to \code{"both"}.

(Missing values of \code{value_var} do not contribute to the boxplot itself,
irrespective of this argument.)}

\item{count_style}{Character string controlling how counts are displayed
when \code{show_counts = TRUE}. Options are:
\itemize{
\item \code{"non-missing"}: show number of non-missing responses
\item \code{"total"}: show total number of responses
\item \code{"both"}: show both as "(non-missing/total)"
}
Defaults to \code{"non-missing"} if \code{na.rm = TRUE}, otherwise \code{"both"}.}

\item{valueLabText}{Optional title for the value variable axis. If \code{NULL}
(default) the title is removed; if \code{""} the name of \code{value_var} is used.}

\item{value_percent_labels}{Logical. If \code{TRUE}, format labels on the numeric
value axis as percentages. Defaults to \code{FALSE}.}

\item{value_percent_scale}{Character string controlling the interpretation
of values when \code{value_percent_labels = TRUE}. One of:
\itemize{
\item \code{"percent"} (default): values are already percentages on a 0--100 scale,
so a percentage sign is appended without rescaling;
\item \code{"proportion"}: values are proportions on a 0--1 scale and are
multiplied by 100 for display.
}
This argument has no effect when \code{value_percent_labels = FALSE}.}

\item{groupLabText}{Optional title for the group variable axis. If \code{NULL}
(default) the title is removed; if \code{""} the name of \code{group_var} is used.}

\item{omitGroupLabels}{Optional logical controlling whether to omit group labels.
Helpful when group_var=NULL. Default FALSE.}

\item{titleText}{Optional plot title.}

\item{base_size}{Positive number (default \code{14}) being the base size
(in points) of text in the plot, passed to underlying theme.}

\item{colour}{Boxplot outline colour (any valid R colour). Defaults to \href{1}{get_OME_colours}.}

\item{separate_at}{Optional integer specifying where to draw a horizontal
separation line between groups defined by \code{group_var}.
\itemize{
\item If positive n the separation line is after the first n categories
\item If negative n the separation line is before the n-th last category.
Default \code{NULL} or 0 omits the line.
}}

\item{group_label_width}{Optional integer. Width (in characters) used when
wrapping group labels with \code{stringr::str_wrap()}. Default is \code{NULL}, to not wrap.}

\item{group_labels}{Optional named character vector providing alternative
labels for the grouping variable. Should be of the form
\code{c(level1 = "Label 1", level2 = "Label 2")}. Default is \code{NULL}, which
uses the factor's existing levels.}

\item{...}{Additional arguments passed from \code{\link[=OME_boxplot]{OME_boxplot()}} to
\code{OME_boxplot_()}. Not used when calling \code{OME_boxplot_()} directly.}
}
\value{
A ggplot object
}
\description{
\code{OME_boxplot()} builds a boxplot from survey-style data using \strong{bare column names}.
\code{OME_boxplot_()} is the programmatic interface and underlying engine,
designed for use when column names are supplied as \strong{strings} or \strong{symbols}.
}
\details{
Features include:
\itemize{
\item optional subdivision into separate boxes,
\item optional labels showing the number of non-NA responses per box,
\item optional dashed line to visually separate some of the boxes.
}

\code{OME_boxplot()} uses tidy evaluation;
\code{OME_boxplot_()} uses standard evaluation and is safe for loops and
programmatic workflows.
}
\note{
Future/possible extensions include
\itemize{
\item support for dodging/colour
\item maybe vertical/horizontal switching
\item maybe faceting
}
}
\examples{
dat <- tibble::tibble(
  Score = c(5, 7, 6, 9, 4, NA),
  Group = factor(c("Alpha", "Alpha", "Alpha", "Beta", "Beta", "Beta")),
  Grouping2 = factor(c("G1", "G2", "G1", "G2", "G1", "G2"))
)

# Basic example
OME_boxplot(dat, Score, Group)

# Without counts
OME_boxplot(dat, Score, Group, show_counts = FALSE)

# Show only the number of non-missing values contributing to each box
OME_boxplot(dat, Score, Group, count_style = "non-missing")

# With a title
OME_boxplot(dat, Score, Group, title = "Example boxplot")

# Change base font size (e.g. for a presentation)
OME_boxplot(dat, Score, Group, base_size = 18)

# Example programmatic use,
# supposing here that dat includes variables Score, Group and Grouping2
\dontrun{
  group_vars <- c("Group", "Grouping2")
  for (v in group_vars) {
    p <- OME_boxplot_(
      data      = dat,
      value_var = "Score",
      group_var = v
    )
    print(p)
  }
}

}
\author{
Dave Sirl
}
